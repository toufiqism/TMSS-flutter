name: tracgo
description: "TracGo — Transport Management System"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.1+2003

environment:
  sdk: ^3.12.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Referenced by the Flutter framework's Cupertino library even though this app uses
  # Material everywhere. Without it, release builds warn that the CupertinoIcons font
  # is missing. Unused glyphs are tree-shaken away regardless.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.4.2
  go_router: ^17.5.0
  dio: ^5.11.0
  freezed_annotation: ^3.1.0
  flutter_secure_storage: ^11.0.0
  # Non-sensitive local flag only (see SessionLocalDataSource): it is wiped on
  # uninstall, which is precisely what the Keychain is not.
  shared_preferences: ^2.5.5
  intl: ^0.20.3
  firebase_core: ^4.13.0
  firebase_crashlytics: ^5.2.7
  firebase_remote_config: ^6.5.6
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Drives the real app on a real device/simulator. Present for one job: capturing the
  # App Store screenshots at native 6.9" resolution. This Mac cannot inject taps into
  # the iOS Simulator (no idb, no assistive access, `simctl` has no input verbs), so the
  # Android trick of `adb shell input tap` has no iOS counterpart — the app has to drive
  # itself from inside. See tool/store/README-ios-screenshots section in
  # docs/appstore/graphics/README.md.
  integration_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  freezed: ^3.2.5
  mocktail: ^1.0.5

  # Virtual clock for the password-reset countdowns. `PasswordResetNotifier` runs a real
  # `Timer.periodic`, and draining its 60-second resend cooldown in real time would cost
  # a minute per test. Already present transitively via flutter_test; declared here
  # because `depend_on_referenced_packages` (rightly) refuses an undeclared import.
  fake_async: ^1.3.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # Space Grotesk + Plus Jakarta Sans replaced Manrope + Inter with the Sign In redesign.
  # Google ships both only as variable fonts; these static instances were cut from the
  # wght axis with fontTools (see README "Fonts"), matching the per-weight file layout
  # the project already used.
  fonts:
    - family: Space Grotesk
      fonts:
        - asset: assets/fonts/space_grotesk_regular.ttf
          weight: 400
        - asset: assets/fonts/space_grotesk_medium.ttf
          weight: 500
        - asset: assets/fonts/space_grotesk_bold.ttf
          weight: 700
    - family: Plus Jakarta Sans
      fonts:
        - asset: assets/fonts/plus_jakarta_sans_regular.ttf
          weight: 400
        - asset: assets/fonts/plus_jakarta_sans_medium.ttf
          weight: 500
        - asset: assets/fonts/plus_jakarta_sans_semibold.ttf
          weight: 600
        - asset: assets/fonts/plus_jakarta_sans_bold.ttf
          weight: 700
