<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required in the main manifest, not just src/debug: the Flutter template only
         declares it for debug/profile, so a release build would have no network access
         at all once the app talks to https://tms.carcopolo.com/bt/api. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- android:enableOnBackInvokedCallback is load-bearing, not boilerplate. This app
         targets SDK 36, where the platform uses the predictive back API and stops calling
         the legacy Activity.onBackPressed() path. Flutter's engine only registers an
         OnBackInvokedCallback when this flag is set, so without it the system back gesture
         never reaches the framework at all: every PopScope in the app is inert, and "back
         on the requisition list returns to the dashboard" silently does nothing on device
         while still passing its widget test — tester.binding.handlePopRoute() drives the
         framework directly and bypasses the platform. -->
    <application
        android:label="TracGo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
