gradle-wrapper.jar
/.gradle
# Kotlin 2.x build-session scratch dir, created by the Gradle build. Build
# output, not source.
/.kotlin/
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
# Catches duplicate copies too — Windows names a re-download "key (1).properties",
# which the literal rule above does not match, and one was staged by accident.
key*.properties
!key.properties.example
**/*.keystore
**/*.jks
